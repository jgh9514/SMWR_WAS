# 로컬/개발 프로필 — Log4j2 설정 고정 (application.yml 의 jdbc.sqlonly:OFF 덮어씀)
#
# Quartz(배치): application.yml 기본 OFF. 로컬 스케줄은 spring.profiles.active 에 local 포함(예: dev,local).
# dev 만 쓰면 Quartz 비활성 → SMW_BATCH_QUARTZ_ENABLED=true 로 켜거나 local 추가.
#
logging:
  config: classpath:log4j2-spring.xml
  level:
    jdbc.sqlonly: INFO
    jdbc.sqltiming: DEBUG
    log4jdbc.log4j2: DEBUG
    com.sysconf.interceptor.MybatisInterceptor: DEBUG

spring:
  task:
    scheduling:
      enabled: ${SMW_SCHEDULER_ENABLED:false}
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: ${SPRING_DATASOURCE_URL:jdbc:log4jdbc:postgresql://13.236.20.39:30432/summonerswar}
    hikari:
      leak-detection-threshold: ${SPRING_DATASOURCE_HIKARI_LEAK_DETECTION_THRESHOLD:0}
      username: ${SPRING_DATASOURCE_USERNAME:postgres}
      password: ${SPRING_DATASOURCE_PASSWORD:}
  batch:
    jdbc:
      initialize-schema: never
      platform: postgresql
  mail:
    enabled: ${SPRING_MAIL_ENABLED:false}
    username: ${SPRING_MAIL_USERNAME:}
    password: ${SPRING_MAIL_PASSWORD:}
    from: ${SPRING_MAIL_FROM:noreply@example.com}
    properties:
      mail:
        debug: true

smw:
  mybatis:
    log:
      inline-sql: true
  # 개발 DB: 운영(application.yml)의 통계·보유몬 스킵을 덮어 전체 통합 Job 검증
  rta:
    batch:
      skip-monster-stats-in-unified-job: false
      skip-user-monster-owned-agg-in-unified-job: false
  security:
    jwt-signing-secret: ${SMW_JWT_SIGNING_SECRET}
    jwt-encryption-key: ${SMW_JWT_ENCRYPTION_KEY}
  service-domain: localhost, localhost:3000, jgh9514.github.io
  front-url: http://localhost:3000
  server-url: http://localhost:8080
  cookie-domain: jgh9514.github.io
