# K8s smw-batch 전용. SPRING_PROFILES_ACTIVE 에 quartz-jdbc 포함 시만 로드.
# docker 프로필과 공존(예: docker,quartz-jdbc). smw-app 은 docker 만 — Quartz RAMJobStore, 아래 org.quartz.jobStore.* 병합 없음.
---
spring:
  config:
    activate:
      on-profile: quartz-jdbc
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      # JobStoreTX 는 Quartz 명명 DataSource 요구( DataSource name not set ). Spring JDBC 스토어는 이 클래스(또는 미설정+Boot 주입) 사용.
      # job-store-type 이 jdbc 가 아니면 RAMJobStore + tablePrefix 등 → "RAMJobStore props could not be configured" 가능.
      org.quartz.jobStore.class: org.springframework.scheduling.quartz.LocalDataSourceJobStore
      org.quartz.scheduler.instanceName: smwQuartz
      org.quartz.scheduler.instanceId: AUTO
      org.quartz.jobStore.isClustered: true
      org.quartz.jobStore.driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
      org.quartz.jobStore.useProperties: false
      org.quartz.jobStore.tablePrefix: QRTZ_
      org.quartz.jobStore.misfireThreshold: 120000
      # 배치 동시 실행 수 제한 — 커넥션 풀(28) 고갈 방지. 무거운 Job(COPY/MERGE)이 커넥션을 오래 점유하므로 보수적으로 설정
      org.quartz.threadPool.threadCount: ${SMW_QUARTZ_THREAD_COUNT:4}
