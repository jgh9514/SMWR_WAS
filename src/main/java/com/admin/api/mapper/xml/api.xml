<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.api.mapper.ApiMapper">

	<select id="selectApiList" parameterType="sqlMap" resultType="sqlMap">
		/* selectApiList — sys_api 제거 후 빈 목록 */
		SELECT NULL::varchar AS api_id WHERE FALSE
	</select>

	<select id="selectApiRoleList" parameterType="sqlMap" resultType="sqlMap">
		/* selectApiRoleList — sys_api 제거 후 빈 목록 (ApiMapper 시그니처 유지) */
		SELECT NULL::varchar AS api_id WHERE FALSE
	</select>

	<select id="selectApiKey" parameterType="sqlMap" resultType="string">
		/* selectApiKey */
		SELECT NULL::varchar AS api_id WHERE FALSE
	</select>

	<update id="insertApiList" parameterType="sqlMap">
		/* insertApiList — sys_api 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<update id="updateApiList" parameterType="sqlMap">
		/* updateApiList — sys_api 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<update id="deleteApiList" parameterType="sqlMap">
		/* deleteApiList — sys_api 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<update id="deleteApiRole" parameterType="sqlMap">
		/* deleteApiRole — sys_api_role 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<update id="insertApiRole" parameterType="sqlMap">
		/* insertApiRole — sys_api_role 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<update id="updateApiRole" parameterType="sqlMap">
		/* updateApiRole — sys_api_role 제거 후 no-op */
		UPDATE sys_role SET upt_date = upt_date WHERE FALSE
	</update>

	<select id="selectApiByUrl" parameterType="sqlMap" resultType="sqlMap">
		/* selectApiByUrl */
		SELECT NULL::varchar AS api_id WHERE FALSE
	</select>

</mapper>
